---
import BaseLayout from "../layouts/BaseLayout.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import { jobs } from "../data/jobs";

const applyHref = (contact: string, title: string) => `mailto:${contact}?subject=${encodeURIComponent(`Bewerbung: ${title}`)}`;
const firstSentence = (description: string) => {
  const sentence = description.match(/^[^.]+[.]/)?.[0];
  return sentence ?? description;
};
---

<BaseLayout
  title="Jobs"
  description="Arbeiten bei Ihre Pflege. Aktuelle Stellenangebote in der ambulanten Pflege in Bayern."
>
  <Nav />
  <main class="page-main">
    <section class="section jobs">
      <div class="container page-intro">
        <p class="eyebrow" data-reveal>Karriere</p>
        <h1 data-reveal style="--motion-delay: 80ms">Arbeiten bei Ihre Pflege</h1>
        <p data-reveal style="--motion-delay: 160ms">
          Pflege ist sinnvolle Arbeit. Wir suchen Menschen, die gerne pflegen und Teil eines verlässlichen Teams werden
          möchten: in Voll- oder Teilzeit, als Fachkraft oder Quereinsteiger.
        </p>
      </div>

      <div class="jobs__inner container">
        <div class="job-list">
          {jobs.map((job, index) => (
            <article class="job-card card" data-reveal style={`--motion-delay: ${index * 100}ms`}>
              <div class="job-card__content">
                <h3>{job.title}</h3>
                <p class="job-card__entity">{job.entity}</p>
                <p class="job-card__meta">
                  {job.type} · {job.department}
                </p>
                <p class="job-card__excerpt">{firstSentence(job.description)}</p>
              </div>
              <a class="btn-primary" href={applyHref(job.contact, job.title)}>Jetzt bewerben</a>
            </article>
          ))}
        </div>
        <p class="jobs__note">
          Sie finden keine passende Stelle? Schreiben Sie uns trotzdem:
          <a href="mailto:jobs@ihre-pflege.de">jobs@ihre-pflege.de</a>
        </p>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>

<style>
  .jobs__inner {
    margin-top: 2.5rem;
  }

  .job-list {
    display: grid;
    gap: 1rem;
  }

  .job-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.5rem 2rem;
  }

  .job-card h3 {
    margin: 0 0 1rem;
    font-size: 1.65rem;
    line-height: 1.15;
  }

  .job-card__entity {
    color: var(--teal);
    font-weight: 500;
  }

  .job-card__meta {
    color: var(--text-muted);
    font-size: 0.92rem;
  }

  .job-card__excerpt {
    margin-top: 0.85rem;
    max-width: 62ch;
    line-height: 1.75;
  }

  .jobs__note {
    margin-top: 2rem;
    color: var(--text-muted);
    text-align: center;
  }

  .jobs__note a {
    color: var(--teal);
    font-weight: 500;
  }

  @media (max-width: 720px) {
    .job-card {
      align-items: stretch;
      flex-direction: column;
    }
  }
</style>
